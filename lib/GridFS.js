var GridFSProvider = function( options ) {
  
};

GridFSProvider.prototype.save = function( attachment , callback ) {
  
};

GridFSProvider.prototype.remove = function( attachment , callback ) {
  
};

module.exports = GridFSProvider;
